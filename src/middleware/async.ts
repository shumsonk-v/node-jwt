// import { NextFunction, Request, Response } from 'express';

// const asyncMiddleware = (fnc: (req: Request, res: Response, next: NextFunction) => {}) => {
//   return (request: Request, response: Response, nextFunc: NextFunction) => {
//     Promise.resolve(fnc(request, response, nextFunc)).catch(nextFunc);
//   }
// };
